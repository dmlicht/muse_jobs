from distutils.core import setup

setup(name='muse_jobs',
      version='0.1',
      description='Download all jobs from the muse jobs endpoint',
      author='David Lichtenberg',
      author_email='hello [at] davidlichtenberg [dot] com',
      packages=['muse_jobs'],
     )
